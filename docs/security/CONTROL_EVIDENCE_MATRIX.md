# Control Evidence Matrix

Status: living claim registry; selected rows now have spike evidence, while product controls remain proposed until implementation and adversarial validation.

No security claim advances because it appears in architecture text. A claim becomes implemented
only when its mechanism exists, and validated only when exact attack tests and retained evidence
cover the pinned configuration.

Status values:

- `proposed`: design only;
- `spike-observed`: observed in a named non-production prototype/environment;
- `implemented`: mechanism exists but authoritative validation is incomplete;
- `validated`: exact pinned configuration passed the required retained tests;
- `degraded`: formerly supported claim cannot currently meet its evidence/freshness requirement;
- `unsupported`: mechanism is unavailable and execution requiring it must refuse or the contract
  must change.

| Claim ID | Claim | Threat | Owner | Mechanism | Live verification | Required attack tests | Receipt/transcript evidence | Known limitation | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AUTH-001 | One approval authorizes at most one attempt | Replay, concurrent double execution | Broker + Supervisor | Attempt nonce plus durable atomic grant consumption | Grant-ledger transaction and restart reconciliation | Replay, concurrent execute, crash after consumption | Grant digest, consumption event, attempt ID | Model/process-kill evidence passed; durable product ledger remains unimplemented and denial of service can burn approval | spike-observed |
| AUTH-002 | Daemon cannot approve or launch | Compromised agent-facing service | Broker + Supervisor + macOS | Keychain isolation, XPC peer requirements, Supervisor-only backend endpoint | Peer/key access checks | Daemon key use, IPC impersonation, direct launch attempts | Integrity assessment peer results | Gate B observed daemon key denial and XPC impersonation denial; backend endpoint/product topology pending | spike-observed |
| PLAN-001 | Executed bytes equal registered/approved plan | Substitution after approval | Supervisor + Broker | Exact durable registration; execute by ID only; independent digest | Registration lookup and staged digest checks | Plan A/B substitution, modified bytes, stale registration | Plan digest, registration ID/sequence, grant binding | Correct signed logic still trusted | proposed |
| SAFE-001 | V0 unsupported powers cannot be enabled by daemon policy | Policy bug or daemon compromise | Supervisor | Versioned non-overridable hard-safety validator | Independent plan check before registration/attempt | Network/process/env/package/image/path fields and type confusion | Validation version and result | Validator correctness remains TCB | proposed |
| RUNTIME-001 | V0 workload cannot use subprocess, native addon, FFI, inspector, macro, environment-file, or package-install powers | Runtime API bypass of approved authority | Supervisor + runtime profile + trusted launcher | Exact externally enforced runtime/launcher profile; mechanism unresolved | Construction-level native-capability/syscall/module-path closure, governed source diff, deliberate capability-restoration mutations, runtime/profile identity, and prohibited-power probes | Direct and aliased APIs, workers/children, FFI/native loading, inspector, dynamic import/install, config/environment discovery, descriptor inheritance, restored primitive | Runtime bundle/profile/source/patch digests and corpus result | Stock Bun 1.3.14 exposes subprocess and FFI APIs and no non-bypassable disable profile has been evidenced; a finite API corpus cannot prove absence, so execution requiring this contract must refuse or the contract must change by ADR | unsupported |
| RUNTIME-002 | Guest runtime/root bytes equal the admitted immutable bundle | Same-user mutation or substitution of the live runtime root | Supervisor + runner + backend | Proposed three-part custody: finalized object with no writable aliases, retained genuine read-only descriptor, and stable `/dev/fd/N` attachment | Final descriptor `F_GETFL` access mode plus device/inode/type/permission/link/length/digest, guest-observed digest, and no pathname recovery | Create/open race, path/link/mapping/write/truncate, wrong/reused FD, shared-offset/status behavior, debugger/task-port attempt, runner/Supervisor crash | Runtime manifest digest, finalized custody record, transfer mechanism, guest digest, recovery disposition | Guest-read-only root mechanics were observed, but final-object construction, `/dev/fd/N`, both libkrun opens, exact App Sandbox profile, and adversarial end-to-end custody are unproven | proposed |
| RI-001 | Trusted peers match enrolled component identity | Same-user impersonation, partial update | macOS + Supervisor/Broker | XPC peer requirement, exact build/epoch checks | OS-enforced connection/message check and preflight | Unsigned, wrong ID/team, stale/debugged peer | RuntimeIntegrityAssessment | Apple Development/Developer ID and installed per-user crash/replacement matrices passed; distribution package, cross-session, logout/login, and product epoch store remain | spike-observed |
| KEY-001 | Daemon cannot use Approval or Supervisor evidence key | Authority collapse | Keychain + component identity | Component-specific security-epoch access group/access control | Failed key operation from daemon/stale component | Direct, injected, stale-release, and transition-crash attempts | Key authorization IDs/fingerprints; no secret material | Daemon/sibling and old/new group denial plus crash-safe transition observed; installed distribution/locked-Keychain/migration remain | spike-observed |
| DATA-001 | Daemon cannot retrieve user-only content | Agent content bypass | Content Broker | Separate store/API plus attempt-scoped handles | Credential/ACL/handle checks | MCP/daemon read, forged/stale/cross-job handle | Delivery audit record and content digest | Multi-process ledger denied daemon role and exposed only opaque references; signed product endpoint remains | spike-observed |
| DATA-002A | Broker snapshot bytes equal the selected/approved content identity before guest transport | Live original-file mutation, substitution, or retained mapping | Broker | Fresh Broker-owned snapshot/content object and manifest digest | Descriptor/snapshot identity and digest before grant/transfer | Original-file mutation, links/special files, retained writable descriptor/mapping, Broker crash | Source/input snapshot manifests and custody events | Broker descriptor/snapshot mechanics passed conditionally; product endpoint, installed store, and composition remain | spike-observed |
| DATA-002B | Guest source/input equals registered/approved snapshot bytes | Truncation, substitution, launcher/path staging race, or cross-attempt port confusion | Supervisor + trusted launcher + backend | Proposed separate fixed-cap attempt-bound directional envelopes and immutable launcher-to-child handoff | Per-role caps/binding/digest, exact byte accounting without EOF dependence, guest-observed digests, and child manifest | Partial/extra/wrong-order bytes, wrong/stale attempt, port swap, early child start/read, writer/reader/launcher crash, pathname substitution, child FD inheritance, cross-job reuse | Source/input manifests, port/child identities and bounds, transfer events, guest digests | Exact libkrun multiport transport, launcher binding/handoff, and installed-profile corpus are unproven; runtime-root custody is tracked separately by RUNTIME-002 | proposed |
| NET-001 | Guest has no unauthorized network/IPC | Exfiltration, metadata/host-service access | Supervisor + backend | Proven no-interface/network-none and no host socket mechanism | Exact config inspection plus guest probes | TCP/UDP/DNS/IPv4/6/loopback/Unix/vsock/metadata | Backend config/capability/validation digest | libkrun/HVF without NET and implicit vsock passed narrow TCP/local-CID probes, but block-root adds a `NullFs` virtiofs device with no configured host directory; complete network/IPC and accepted-device corpus remains | spike-observed |
| VMM-001 | Exact guest-facing VMM device/parser surface is closed and accepted | Malformed guest device traffic reaches an unreviewed VMM parser, host route, or unbounded worker path | Supervisor + runner + backend | Closed typed runner/device list plus governed device removal or explicit acceptance of one exact residual surface | Build-feature and binary-route audit, guest inventory, exact device list, sanitizer/coverage record, attach denial | Restored host-directory APIs, malformed virtio queues/descriptors/FUSE opcodes and lengths, mapping requests, concurrency/reset/cancellation, resource exhaustion, VMM/reader failure | Backend/source/build/profile digests, device inventory, route-closure result, corpus/coverage/crash disposition, limitations | [P0-2 replacement evidence](../../experiments/gate-c-libkrun-adversarial/NULLFS_P0_2.md) found no virtiofs feature toggle and falsified the smallest removal because bootstrap failed; the residual generic virtio/FUSE/worker/overlay/`NullFs` path has no relevant fuzz target, sanitizer/coverage corpus, independent review, or exact installed route-closure evidence | unsupported |
| RES-001 | Required resource controls are externally enforced exactly | Host availability loss | Supervisor + backend/host | Mechanism per supported limit dimension | Capability report and runtime observation | CPU, memory, PID, disk, output, cancellation abuse | Exact approved values, mechanism IDs, violation events | Fixed 4 KiB stream prefixes, wall/cancel scheduling, exact forced kill, fixed scratch image, integer vCPU, and closed RAM profiles were observed; CPU percentage/time, arbitrary RAM, total host memory, host pressure, and composed product enforcement remain unsupported/unvalidated | spike-observed |
| CLEAN-001 | Every created guest is destroyed or explicitly unresolved | Persistent hostile execution | Supervisor + backend | Durable handle/cleanup lease and reconcile lifecycle | Backend enumeration and teardown observation | Crash/cancel/timeout/orphan/partial destroy | Destroy/reconcile events and terminal class | Containerization failed; libkrun exact-process teardown and six installed reparent/reap cases passed with the enrolled launchd profile, but reboot/session/power-loss, failed-signal races, product store composition, and gVisor recovery remain | spike-observed |
| COMPLETE-001 | Ordinary success requires attempt-bound guest-reported completion and integrity evidence | False success after guest/bootstrap failure | Supervisor + distinct trusted launcher + backend | Dedicated fixed-cap typed completion/result port with commit trailer, closed launcher/child/runner FD manifests, and separate input/result/runtime/runner/teardown dispositions | Verify port/FD ownership, separate physical/JSON caps, cap-plus-one continuous drain without EOF dependence, binding, commit ordering after child-tree termination, bounded shutdown, hostile console-control/queue validation, and every required terminal element | Corrupt root, panic/OOM, missing executable, unchecked port ID/event, malformed queue/descriptor, wrong/duplicate/stale/forged/partial/oversized/trailing frame, early/missing commit, partial-then-error/zero host write, unexpected/swapped FD, reader stall/death, `SIGPIPE`, launcher crash before/after commit | Completion digest/type, port accounting, child/runner FD manifests, integrity/result checks, teardown result, guest-reported limitation | Multiple libkrun cases returned runner status zero without successful workload completion; the typed port, distinct launcher, and exact caps are not implemented; pinned upstream has unchecked guest port indexing, non-stop-aware TX waiting, undocumented directionality/shared-status mutation, and partial-error gaps; this is not guest-kernel attestation | proposed |
| EGR-002 | Only bounded safe artifacts leave hostile scratch storage | Symlink/special-file/parser exploit, sparse/size abuse, partial release | Broker + disposable parser | Fixed raw format/profile, bounded parser sandbox, fixed slots, limit-plus-one copy | Filesystem/profile checks and parser lifecycle reconciliation | Malformed ext4, links/devices/xattrs/ACLs, sparse/overlap, parser crash/timeout, partial copy | Parser identity/profile, artifact digest/length, rejection/cleanup class | Spike extractor rejected representative hostile files, but shell/`debugfs`/Docker tooling is non-product and malformed-filesystem fuzzing is open; this gate applies before file artifacts, not the first inline-JSON result | spike-observed |
| SUPPLY-001 | Admitted runtime bytes have complete attributable build and release identity | Dependency/source substitution, unreproducible or unreviewed bytes | Release engineering + trust verifier | Pinned materials, two-builder provenance, SBOM/source bundle, final signed-byte manifest | Admission checklist and exact installed readback | Moving inputs, path variance, missing materials, post-sign mutation, mixed package | Runtime manifest, provenance/SBOM/source digests, validation record | Path-remapped local rebuild feasibility passed; current bytes lack independent builders, complete materials/final manifest, clean-host floor, and exercised operations and are no-go | spike-observed |
| PATCH-001 | Runtime patches are governed, reviewed, and removable | Patch drift or untracked security behavior | Runtime maintainers + reviewers | Governed fork or accepted upstream release with owner, digest, tests, rebase/removal record | Source/patch manifest and review state | Missing/reordered/modified patch, upstream rebase, partial build | Patch digests, upstream reference, review attestation | Two required Capsule patches remain outside an accepted upstream release; governance is designed but not operational | proposed |
| DIST-001 | Installed topology is signed, notarized, read back, and accepted on supported clean hosts | Substitution, quarantine/Gatekeeper failure, unsupported floor | Installer + Installation Trust Domain | Candidate complete notarized app with enrolled manifest and embedded per-user `SMAppService`; exact topology unproven | Staple/Gatekeeper/installation readback, service registration, inherited-descriptor behavior, and host compatibility | Missing ticket, translocation, partial install, old/new mix, service/recovery failure, unsupported OS | Package/runtime digests, ticket/assessment, installation epoch | Same-host Developer ID mechanics passed; pending runner-only notarization does not cover the complete Supervisor/runner/runtime/service topology, and no clean-host/session/floor matrix passed | spike-observed |
| EGR-001 | Agent receives only fixed default summary | Content/metadata exfiltration | Broker + daemon | Fixed typed response with no guest strings | Response schema enforcement | Names/sizes/timing/errors/log/output injection | Summary schema/version and release decision | State/timing channel remains | proposed |
| TRUST-001 | Partial component update fails closed | Stale/mixed components | Installation Trust Domain | Signed manifest, common epoch binding, execution fence, component acceptance | Connection/preflight epoch comparison and durable-state/checkpoint reconciliation | Old/new combinations, process death, disk-full, WAL/corruption, partial/coherent restore | Epoch number/digest, transition/checkpoint, assessment result | Model/process evidence passed; product installer/store pending, coherent rollback needs stronger anchor | spike-observed |
| TUF-001 | External trust data is locally verified without live execution dependency | Malicious/stale service, rollback | Updater/trust verifier | Pinned TUF root plus signed local TrustSnapshot | Version/expiry/hash/checkpoint verification | Freeze, rollback, mix-and-match, delegated-scope abuse | Trust snapshot/checkpoint/freshness | Capsule defines revocation semantics | proposed |
| EVID-001 | User receipt contains attributable approval and enforcement claims | Daemon-forged success | Broker + Supervisor | Purpose-separated signatures over same registration/attempt | Independent signature and binding verification | Remove/swap/replay/cross-object evidence | Embedded grant, transcript, authorizations | Not platform attestation or proof of truth | proposed |

## Update rule

When a mechanism or spike changes, update the corresponding row with an evidence link, exact
version/configuration, and limitations. Never promote an entire backend because one row passes; all
claims required by the requested posture must be validated together.
