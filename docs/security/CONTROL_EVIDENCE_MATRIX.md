# Control Evidence Matrix

The final column reports control/evidence maturity, not work progress. Use
[`STATUS_LANGUAGE.md`](../STATUS_LANGUAGE.md) for `PASSED`, trended `IN_PROGRESS`, `BLOCKED`, and
`NO_GO`. An `unsupported` control can coexist with a passed narrow experiment and an active parent
workstream; do not collapse those dimensions into one label.

Status: living claim registry; selected rows have spike evidence or implemented unwired local
mechanics, while product controls remain proposed until their complete mechanisms and adversarial
validation exist.

No security claim advances because it appears in architecture text. A claim becomes implemented
only when its mechanism exists, and validated only when exact attack tests and retained evidence
cover the pinned configuration.

Status values:

- `proposed`: design only;
- `local-mechanic`: repository code and retained tests implement only the named unwired/no-guest
  mechanic; no consumer or production control is activated;
- `spike-observed`: observed in a named non-production prototype/environment;
- `implemented`: a product mechanism exists but authoritative validation is incomplete;
- `validated`: exact pinned configuration passed the required retained tests;
- `degraded`: formerly supported claim cannot currently meet its evidence/freshness requirement;
- `unsupported`: mechanism is unavailable and execution requiring it must refuse or the contract
  must change.

A row's status covers its whole claim. A `proposed` or `unsupported` row may cite a narrower local
mechanic without promoting the larger production or guest-facing claim.

| Claim ID | Claim | Threat | Owner | Mechanism | Live verification | Required attack tests | Receipt/transcript evidence | Known limitation | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SER-001 | A Go CBOR dependency cannot replace Capsule's raw predecode, canonical-on-wire, closed-object, trusted-binding, key-authorization, or payload-replay controls | Generic CBOR/COSE feature restoration, parser allocation, header/key confusion, equivalent-signature replay split | Protocol maintainers + Supervisor/Broker owners | Exact fxamacker v2.9.2 is reachable only through one unwired object-specific `SourceManifest` codec after Capsule predecode; the handwritten oracle remains and go-cose is rejected for product use | [Production comparison](https://github.com/Shrimpworks/capsule-experiments/blob/0d8233b55f153b27a901a9ec45a3834208e3aa86/experiments/production-cbor-cose-profile/RESULTS.md) plus [v0 object-set/wrapper result](../V0_CBOR_OBJECT_SET_AND_WRAPPER.md): all 19 applicable Go/TypeScript cases, three exact known answers, 10,000 wrapper/oracle round trips, 673,967 30-second fuzz executions, focused race, ownership and restoration probes | Duplicate/unknown/out-of-order fields, tags, indefinite/nonpreferred forms, floats/simple/UTF-8/trailing data, depth/collection/exact/cap+1 resources, wrong type/object/version/domain, and direct generic-mode restoration | None; no receipt/transcript consumer exists | Unwired local wrapper evidence only: signed-object set, Swift production wrapper, Supervisor/Broker same-byte path, live key, COSE, replay ledger, consumer, and product control remain blocked; ADR-0019 remains Proposed | local-mechanic |
| AUTH-001 | One approval authorizes at most one created attempt | Replay, concurrent double attempt creation | Broker + Supervisor | Unwired fixed store keys canonical-payload replay, retains nonce uniqueness, and atomically changes one usable approval to consumed while inserting one immutable `created` attempt | `TestApprovalAttemptDurableIdempotentAtomicHappyPath`, `TestApprovalAttemptConcurrentExactRequestsConverge`, and reopen validation | Exact/equivalent replay, concurrent submit/request, confirmed and indeterminate commit, corruption/cross-link reopen | Planned grant digest, consumption event, attempt ID | Local file-snapshot mechanic only: fixture verifier, injected role context/integrity assessor, no production crypto/authorization, no multi-process locking or archive/compaction, and no consumer; denial of service can burn approval | local-mechanic |
| AUTH-002 | Daemon cannot approve or launch | Compromised agent-facing service | Broker + Supervisor + macOS | Keychain isolation, XPC peer requirements, Supervisor-only backend endpoint | Peer/key access checks | Daemon key use, IPC impersonation, direct launch attempts | Integrity assessment peer results | Gate B observed daemon key denial and XPC impersonation denial; backend endpoint/product topology pending | spike-observed |
| STATE-001 | Unwired approval/attempt authority state fails closed on replay, effective-time rollback, capacity, injected commit faults, and corrupt reopen | Replay resurrection, split consume/create, ambiguous durability, state corruption | Supervisor | One versioned fixed snapshot colocates registration, durable time high-water, approvals, attempts, indexes, and set digests; confirmed abort preserves pre-state and indeterminate outcome fences mutation until reopen | `TestApprovalSubmissionTimeBoundaries`, `TestApprovalAttemptFaultAndProcessDeathMatrix`, `TestApprovalAttemptReopenRejectsCorruptionAndCrossLinks`, `TestApprovalAttemptHighWaterAndAttemptCapacity`, `TestApprovalCapacityAndNonEvictingRetainedState`, and 12 Slice B manifest cases | Equality/rollback, nonce/payload replay, exact/cap-plus-one active and retained ceilings, missing half/cross-link/digest/version corruption | None; evidence/transcript profile is not implemented | Fixed no-eviction test store only; lacks production archive/compaction, multi-process locking, rollback-resistant uniqueness, backup/restore, consumers, and continuous-service retention | local-mechanic |
| LIFE-001 | Committed created attempts enter and recover through an `AttemptID`-keyed no-guest fake lifecycle without binding substitution or effect replay | Registration-key conflation, Plan A/B substitution, one approval driving two attempts, replayed fake effects | Supervisor | `AttemptResolver`, `Drive(AttemptID)`, `Recover(AttemptID)`, exact plan/binding revalidation, colocated durable lifecycle records, stable effect/instance identity, attempt-keyed fake faults, and hard-coded `FakeBackend.CreatesGuest() == false` | Focused `registeredlifecycle` tests cover binding rejection before prepare, distinct attempts per registration, exact/concurrent replay, every fake fault/effect boundary, reopen recovery, repeated/concurrent startup, expiry independence, and owner/coordinator mismatch | Missing/mutated/cross-linked attempt, concurrent/sequential replay, aliased attempt ID, fake before/after-effect faults, interruption after each effect, expiry during recovery, repeated startup | None; no product transcript or success result exists | Fixed no-eviction snapshot and injected in-process coordinator only; fake effects are not real backend/guest evidence; no consumer, content, runtime, real backend, or guest | local-mechanic |
| LIFE-002 | Durable lifecycle intent, effect identity, cleanup, capacity, and startup recovery share one Supervisor transaction domain | Split-store ambiguity, lost effect response, duplicate lifecycle owner/effect, cleanup loss after restart, unsafe capacity release | Supervisor | Proposed ADR-0025 colocated lifecycle record, stable per-operation effect permit, before/after-effect checkpoints, injected owner/startup coordination, exact reconciliation, retained unresolved/repair state, and joined active-capacity accounting | Fixed-store v1 reopen validation plus per-attempt record/version/effect/instance checks; `TestFixedStoreV1ExactActiveCapacityReleasesOnlyAfterDurableDestroy`, `TestFixedStoreV1ExactRetainedLifecycleCapacityNeverEvicts`, `TestConcurrentRepeatedStartupUsesOneInjectedOwnerAndOmitsTerminal`, and `TestRepeatedStartupRetainsExhaustedRecoveryWithoutRestart` | Confirmed/indeterminate commit at every effect boundary, process death, ENOSPC/I/O, corruption, lost response, stale/missing/unknown backend state, exact 256/4,096 cap and cap-plus-one, observed/stopped/destroy-confirmed/unresolved/quarantined non-release, repeated startup, exhausted recovery, owner mismatch | None; evidence/transcript profile is not implemented | Slices E1-E5 are an unwired local mechanic. The 4,096-record population is 30,321,818 bytes under a v1-only 64 MiB raw bound; there is no archive/compaction, real multi-process/platform lock, rollback/backup, production reconciliation, consumer, content, evidence, runtime, real backend, or guest. ADR-0025 remains Proposed | local-mechanic |
| OWNER-001 | One enrolled Supervisor owns installation state before store, recovery, archive, or adapter work | Duplicate Supervisor, inherited lock descriptor, stale/replaced lock path, split store mutation | Installer + Supervisor + macOS | Proposed ADR-0033 pre-created enrolled sibling object, exact descriptor/path identity checks, nonblocking BSD `flock`, lifetime opaque `CLOEXEC` descriptor, owner-required current-v1 opener, same-session store/coordinator, post-open fence, and ordered close | G1 tests plus the bounded G2 process/fault corpus check closed enrollment, exact descriptor flags, contention, descriptor lifetime, exec/inheritance, process death/reopen, replacement/reuse/close faults, wrong owner session, ownership-before-store-before-sorted-recovery order, zero fake calls on pre-store refusal, response-loss recovery, and `FakeBackend.CreatesGuest() == false`; G3 retains a closed identity/bootstrap/update fixture and exact certificate/profile NO-GO only | Missing/symlink/wrong mode/UID/type/link/dev/inode, independent owners/descriptors, fork/exec inheritance, crash/reopen, rename/unlink/replacement before and after open, descriptor reuse, wrong session, corruption, recovery fault/response loss, concurrent startup, update/reboot, protected-root bypass | None; the lock is not receipt evidence | Unwired local mechanic under owned temporary roots and the current fixed v1/no-guest path only. G3 stopped before installed build because the authorized certificate emits Team `3DDR84M4JS`, not expected W4, and no W4 profile is cached. No authenticated signed bootstrap, selected protected-root creation ceremony, descriptor-relative installed store open, v2/archive composition, migration/backup/repair port, production service, or same-UID containment exists. Advisory locking can be ignored or path-replaced by an actor with directory access; no cross-user, rollback, continuous-service, authenticated-IPC, or production claim | local-mechanic |
| STATE-002 | Closed Supervisor authority cohorts archive without replay resurrection, identifier reuse, cleanup loss, or ambiguous activation | Capacity-driven eviction, split hot/archive state, missing referenced history, coherent rollback, archive/index substitution | Supervisor | Proposed ADR-0031 immutable full-record registration cohorts, exact registration/approval/attempt/nonce/effect/instance/replay tombstone indexes, domain-separated set/index/checkpoint digests, segment-publish-before-active-reference ordering, and full fail-closed reopen | F1 verifies passive types, exact limits/known answers, defensive copies, deterministic eligibility, multi-attempt cohort atomicity, and recovery exclusion. Passive/stateful F2 verifies scope-separated indexes, typed locations/counts, lifecycle absence, exact migration genesis, all-hot reconstruction, downgrade refusal, and bounded corruption/capacity/concurrency/death oracles. F3 verifies one immutable complete-cohort segment, publish-before-reference activation, old-or-new reopen, owner/fault/response-loss/death boundaries, missing/corrupt/substituted segment refusal, and valid-orphan reporting. F4A verifies fresh retained-global registration/approval/attempt/nonce/effect/instance/replay routing, hot/archive semantic equality, all eight passive collision domains, hot-only `AttemptID` recovery, cap/copy/concurrency/reopen/read-fault/substitution/restoration refusal without fallback or rewrite | F4B atomic v2 mutation/new effect tombstones and mutation replay faults, F4C second-segment and 64-segment/index/byte exact growth caps, real power loss, installed duplicate owner, coherent backup/restore/rollback | Migration-genesis and activation checkpoints, one immutable segment, generations, counts, set/index digests, bounded fixed verification result, and location-independent retained lookup results; no receipt profile exists | F1-F4A local conformance evidence only. F4A is read-only and reserves nothing; no v2 authority/lifecycle mutation, new effect tombstone, second segment, adapter call, production engine, installed lock, APFS/real-power-loss evidence, restore activation, anti-rollback anchor, continuous service, consumer, content, evidence, runtime, backend, or guest exists. Referenced history is not deletable; the fixed oracle remains capped and eventually refuses | proposed |
| DAEMON-001 | Agent-facing request handling stays within a configured aggregate service envelope and sheds overload closed | Authenticated-client flood, parser amplification, queue growth, downstream stall | Daemon | Bounded connections, concurrency, in-flight bytes, queues, deadlines, cancellation propagation, per-peer/global accounting, and diagnostics | Aggregate counters and deterministic overload classification | Concurrent maximum-size requests, churn, slow/partial bodies, cancellation races, downstream stalls, queue saturation, high-cardinality errors | Daemon configuration/build identity, peak accounting, overload result, no-authority-change result | Per-request protocol caps exist, but no product-wide concurrency/backpressure/load corpus exists; a same-user attacker can also exhaust host resources directly, so the claim is bounded non-amplification rather than host availability | proposed |
| SOURCE-000 | First-release source is one exact approved pass-through `main.mjs` and no dependency loader can obtain other bytes | Source substitution after registration/approval; CommonJS/package/static/dynamic import gaining unapproved bytes; manifest/member confusion | Daemon planner + Supervisor + Broker + admitted runtime profile | Accepted ADR-0034 single-member source profile, canonical manifest, atomic plan/bindings/manifest/source registration, Broker exact-byte readback, registration-only staging, and unconditionally refusing module-loader/dynamic-import path | Go/TypeScript independently verify the passive source-byte and canonical SourceManifest foundation. Accepted ADR-0035 V0 retains exact passive frames; V1 invokes the exact parser only in an unwired artifact/test harness and agrees with the division-versus-regexp and all 28 M1 HOLD oracles. ADR-0036 accepts the replacement architecture but adds no product evidence. Supervisor, Broker, staging, and runtime-profile checks remain planned | Wrong member/path/media/entrypoint/length/digest; UTF-8/BOM/newline/Unicode mutation; static/dynamic/import-meta and every relative/bare/node/npm/http/data/blob/file specifier; post-registration byte mutation; cap-plus-one; response loss; loader restoration | Passive source profile/media, member length/content/manifest digests, exact known answers, passive validator frames, and unwired V1 artifact result evidence; plan/registration/attempt/runtime-profile bindings and no-loader validation result remain planned | Passive byte/manifest, Source Validator V0, and unwired V1 artifact evidence only. JobProposal narrowing, product language validation, plan construction, M2/S1, source custody, Broker rendering, authenticated IPC, runtime enforcement, backend, and guest do not exist; no execution or admission claim follows | proposed |
| SOURCE-005 | Pre-plan and pre-approval `.mjs` grammar facts come from independently invoked bounded parsing without placing hostile parser memory in an authority process | Scanner false accepts/refusals; parser recovery hides forbidden nodes; parser compromise/crash reaches daemon, Broker, or Supervisor authority | Daemon + Approval Broker + accepted disposable Source Validator design | Accepted ADR-0035 one-shot exact Oxc 0.140.0 parser/AST/semantic mode, copied bounded input/output, fixed digest/length/node/CommonJS-reference result, no persistent product state/keys/network/path/loader/backend route; any diagnostic/fault/mismatch refuses | V0 observes the fixed protocol and 128 cases. V1 retains exact executable/profile/build/assessment hashes, a 74-dependency offline lock, checksum/source/license/notice/SBOM/provenance inventories, all 28 exact M1 results, diagnostic and restoration cases, same-host byte reproduction, and identity-free linker ad-hoc signature/CodeDirectory evidence. V2 retains fixed local launch/fault oracles and the exact stop: `RLIMIT_AS` refuses, the explicit unbounded mutation preserves ambient file/socket/write/memory authority, and supported App Sandbox child entitlements change V1 bytes. ADR-0036 adds accepted architecture only; installation signatures, independent builder, supported confinement/resource/residue corpus, daemon/Broker binding, and fresh product-process checks remain planned | Regex/division scanner stop, strings/comments/property/method names, template import, static/export-from/dynamic/meta restoration, free versus shadowed CommonJS bindings, every parse/early error, cap+1/UTF-8, partial/duplicate/trailing/oversize output, hang/crash/signal/cancel, reactive-footprint/CPU/file/FD/process exhaustion, ambient file/socket/write probes, wrong role/artifact/version, validator bypass | V0 artifact/profile projection plus actual not-enrolled V1 profile, source digest/length, typed counts/status, exact artifact/build/assessment identities, test-only V2 launch descriptor/resource/fault observations, and zero-effect/no-result refusals; enrolled role-specific signatures, supported sandbox/reactive-resource/residue result, and no-key-operation evidence remain planned | Bounded experiment, passive V0, unwired V1 artifact, test-only blocked V2 checkpoint, and accepted R0 architecture only. No installed role-specific launcher/parser/profile or consumer exists. Cleanup is not confidentiality evidence; reactive monitoring is not a hard peak or host-availability guarantee. No Keychain/Supervisor-state denial, runtime no-loader enforcement, backend, or guest exists. Corpus passage is not semantic equivalence or hostile-input proof | spike-observed |
| SOURCE-006 | The supported macOS Source Validator profile has lower authority than daemon/Broker parents, uses fresh role-specific parser children, and states resource/filesystem residuals honestly | Direct sandbox inheritance exposes parent rights; shared/generic launcher/cache/result; cross-role replay; writable-container persistence; native loading/network/filesystem escape; descendant/orphan; reactive-memory overshoot; old/new relabel or mix | Accepted role-separated Source Validator launcher design + daemon + Approval Broker + macOS | Accepted ADR-0036 two private App-Sandboxed role-specific XPC launchers, matching parser/service/method/artifact profiles, no cross-role state/result, fixed copied frames/pipes, Hardened Runtime and exact launch/library constraints, `posix_spawn` descriptor closure, one direct child per request, bounded combined concurrency, reactive footprint observation, kill/drain/reap, and mandatory request/crash/restart/update/startup cleanup | Supported-profile review compares official Apple mechanisms; public SDK/header inspection; retained unprivileged `task_set_phys_footprint_limit` probe returns `KERN_NO_ACCESS`; Accepted ADR-0036 and passive v1 boundary close R0 architecture/ownership only | Wrong parent/responsible/self/library constraint; wrong private service/role/profile/policy/epoch; daemon/Broker sandbox inheritance; app-group/key/container cross-access; network/out-of-container/store/Keychain/process/attach/dlopen/JIT/DYLD; every FD/Mach/bootstrap inheritance; descendant/orphan; reactive threshold/sample/overshoot/system pressure and combined-role concurrency; residue after request/crash/restart/update/startup; launcher/child/consumer death; mixed version/signature/entitlement/OS/epoch | Planned role-specific v1 bundle/profile digests, signed code/entitlement/constraint identities, exact copied request/result and resource-policy bindings, supported-host baseline/threshold/cadence/maximum overshoot/kill latency, process-group and cleanup inventories; current record is accepted design plus negative SDK observation only | R0 architecture decision `PASSED`; product control `BLOCKED`. Private-XPC reachability, new passive bytes/field authority, unsigned construction, signed installed artifacts, confinement/resource/residue corpus, and both consumers do not exist. Container cleanup is not confidentiality proof; reactive sampling is not a hard peak/exact cap or host-availability guarantee. No signing identity, service, app group, runtime, backend, or guest was used or authorized | proposed |
| SOURCE-001 | TypeScript executable bytes and transformation records are produced and retained by one enrolled pre-registration owner, then independently relationship-validated before registration and approval | Daemon/SDK emission forgery, source-store mutation, path substitution, post-approval transformation | Proposed Source Preparer + Supervisor + Broker | Proposed ADR-0032 method-specific service, exact one-shot Node/Amaro worker, role-namespaced immutable store, copy-only resolution, independent Go/Swift validation, executable-only attempt read, and Broker trusted-not-proven disclosure | Exact package/peer/epoch/store identity, complete role graph and staged executable digest | Wrong peer/profile/options/media/path/role/order/record, every cap-plus-one and copy mutation, response loss, crash/store corruption, stale update, original/transform request at execution, and omitted/misleading approval disclosure | Plan v1 source-role digests, prepared/registered source-set reference, exact profile/options/record-set identities, and disclosure version | Design only: a compromised preparer can select mutually consistent executable bytes and claims before approval. Independent validation proves consistency, not faithful erasure, selected-toolchain execution, or semantic equivalence. No Source Preparer, store, endpoint, governed package, installed identity/sandbox evidence, plan v1 consumer, Broker/Supervisor validator, runtime, backend, or guest exists | proposed |
| SOURCE-002 | Source Preparer is the sole security-authorized reader/writer of its immutable source store under the malicious same-user model | Same-user, daemon, sibling, stale-component, updater, link, replacement, mapping, open-handle, and rollback access to original/executable bytes or lifecycle state | Proposed Source Preparer + macOS + installer | Exact OS-enforced single-member protected container, no shared access group, sealed installer-owned genesis/update descriptor, no-live-path copy API, owner lock, epoch fencing, fail-closed reopen/quarantine | Installed container membership, open descriptor/mapping inventory, exact installation/epoch/store-format binding, protected genesis/update evidence | Open/read/write/link/replace/rename/map from every sibling and unrelated same-user fixture; stale process/descriptor across update; first-open creation; malicious updater request; rollback/mixed epoch; corrupt/missing store | Planned store checkpoint, installation/epoch/format, genesis/update authorization, quarantine and retention disposition | Future-conditional TypeScript claim only. P0 rejected mode bits, pathname secrecy, PID, and same-Team identity as sufficient. No exact protected-container mechanism or installed negative evidence exists; P1 is on hold and is not a first-release dependency under ADR-0034 | proposed |
| SOURCE-003 | One-shot Node/Amaro execution has only bounded transform authority and leaves no process, descriptor, mapping, temporary, diagnostic, or reservation state | Parser compromise inherits parent/store/package authority, loads native code, uses network/process APIs, persists descendants, floods stdio, races cancellation/death, or survives parent crash | Proposed Source Preparer + macOS child profile | Fixed release-owned executable/bootstrap plus exact child sandbox/profile, process handle and immutable code identity, pre-input binding, closed descriptor map, nonblocking bounded pipes, process-tree kill, transaction-owned staging cleanup | Child profile and executable identity, process tree, FD/map/temp inventory, reserved bytes, bounded diagnostics, parent/child death disposition | Store/package/filesystem/network/process/attach/native-loading probes; argv/env/cwd/FD mutation; stdout/stderr flood; cap-plus-one; timeout/cancel; pre-bind/parent death; PID reuse; stale temp/map/descriptor | Planned worker/profile identity, reservation peak, fixed result, cleanup/death disposition, no-authority-state-change result | Fixed argv/environment/cwd/descriptors do not prove confinement. Until exact negative evidence exists, Node/Amaro is full Source Preparer/store TCB; no worker or platform profile exists | proposed |
| SOURCE-004 | Source-set lifecycle, release, archive, update, and recovery never invent, duplicate, lose, or decrement authority on refusal, response loss, cancellation, crash, capacity, or epoch transition | Orphan prepared sets, split registration/retain, unauthorized blob release, archive omission, empty-store recreation, rollback, stale writer, ambiguous cleanup | Proposed Source Preparer + Supervisor + installer/archive owner | Intent-bound prepare/resolve/abort/retain/read/release state machine, trusted-time expiry only for prepared state, idempotent replay, quarantine, sealed genesis/migration, selected durable archive/retention release authority | Exact lifecycle/store graph, Supervisor intent/registration truth, archive/hold authority, epoch/store checkpoint, cleanup inventory | Every pre/post-transition fault, response loss, cancellation, cap-plus-one, startup corruption, unknown reference, stale connection/writer, mixed epoch, release without archive/hold discharge, tombstone/reference underflow | Planned intent/state/tombstone/store/archive/epoch evidence and fixed no-authority-state-change refusal | P1 may freeze refusal/quarantine/tombstone objects but not positive blob decrement until one archive/retention rule is selected. No Source Preparer lifecycle or archive composition exists | proposed |
| UI-001 | UI activation alone cannot create approval; grant signing requires the configured user-presence key operation over the exact registered binding | Synthetic input, Accessibility automation, overlay/focus substitution, misleading approval attribution | Approval Broker + macOS | Supervisor-fetched typed view plus LocalAuthentication/Keychain-gated Approval-key operation; explicit elevated-adversary posture | Key operation result, registered binding, peer/session/window posture where supported | CGEvent/synthetic click, signing without presence, stale/focused wrong window, overlay, Accessibility automation, wrong Supervisor/session | Grant binding, key authorization/user-presence policy identifier, elevated-posture limitations | Exact product UI and distribution build do not exist; broad user-granted Accessibility/Screen Recording authority is not claimed contained without separate evidence | proposed |
| PLAN-001 | Executed bytes equal registered/approved plan | Substitution after approval | Supervisor + Broker | Planned exact durable registration, Broker approval, execute-by-ID, and independent staged digest; unwired lifecycle already revalidates exact retained plan and copied bindings before fake prepare | Registration lookup and staged digest checks | Plan A/B substitution, modified bytes, stale registration | Plan digest, registration ID/sequence, grant binding | Local exact-byte registration and fake-preparation rejection exist, but there is no Broker approval, consumer, staging, runtime/backend, executed byte, or guest; correct signed logic remains trusted | proposed |
| SAFE-001 | V0 unsupported powers cannot be enabled by daemon policy | Policy bug or daemon compromise | Supervisor | Versioned non-overridable hard-safety validator | Independent plan check before registration/attempt | Network/process/env/package/image/path fields and type confusion | Validation version and result | Validator correctness remains TCB | proposed |
| RUNTIME-001 | V0 workload cannot use subprocess, native addon, FFI, inspector, macro, environment-file, or package-install powers | Runtime API bypass of approved authority | Supervisor + runtime profile + trusted launcher | Exact externally enforced runtime/launcher profile; mechanism unresolved | Construction-level native-capability/syscall/module-path closure, governed source diff, deliberate capability-restoration mutations, runtime/profile identity, and prohibited-power probes | Direct and aliased APIs, workers/children, FFI/native loading, inspector, dynamic import/install, config/environment discovery, descriptor inheritance, restored primitive | Runtime bundle/profile/source/patch digests and corpus result | The [exact stock Bun 1.3.14 experiment](https://github.com/Shrimpworks/capsule-experiments/blob/0d8233b55f153b27a901a9ec45a3834208e3aa86/experiments/gate-c-bun-runtime-authority/RESULTS.md) observed direct and aliased process, `execve`, FFI/native-loader, inspector, Worker, and descriptor authority despite relevant flags. The [governed-construction review](https://github.com/Shrimpworks/capsule-experiments/blob/0d8233b55f153b27a901a9ec45a3834208e3aa86/experiments/gate-c-bun-runtime-authority/governed-closure/CONSTRUCTION_REVIEW.md) then triggered NO-GO at a 40-hand-authored plus 10-generated-output minimum. The [Deno-family experiment](https://github.com/Shrimpworks/capsule-experiments/blob/0d8233b55f153b27a901a9ec45a3834208e3aa86/experiments/gate-c-deno-runtime-authority/RESULTS.md) also reached NO-GO: full Deno retained initial-graph, Worker, SIGUSR1-inspector, compatibility, and runtime-managed storage routes, while minimal `deno_core` physically registered 99 built-in ops and lacked a TypeScript pipeline. The later [governed `deno_core` physical-omission experiment](https://github.com/Shrimpworks/capsule-experiments/blob/0d8233b55f153b27a901a9ec45a3834208e3aa86/experiments/gate-c-deno-core-physical-omission/RESULTS.md) passed its narrow one-file pre-registration and final-link question, reproduced an ASLR-controlled three-op snapshot/binary, and rejected fixed restoration mutations. ADR-0028 selects that governed candidate as the first engineering direction. Its governed Deno and `rusty_v8` baseline branches are merged, and the [fork-native integration check](https://github.com/Shrimpworks/capsule-experiments/blob/0d8233b55f153b27a901a9ec45a3834208e3aa86/experiments/gate-c-fork-native-deno-runtime-bundle/RESULTS.md) stopped before construction because the merged publication contract had only an amd64 builder. Governed `rusty_v8` PR #4 then merged at head `80e863ddb942a4aa2b384e794fc23e35b9d2bb15`; its [exact-head ARM64 workflow](https://github.com/Shrimpworks/rusty_v8/actions/runs/30925045754) passed network-disabled build/test/evidence collection and unsigned bundle upload. That closes the fork construction blocker but supplies no governed release, accepted Capsule bundle, or admission. No release or profile is admitted. Execution requiring this contract must refuse until fork-native reconstruction and the full composed profile pass. | unsupported |
| RUNTIME-002 | Guest runtime/root bytes equal the admitted immutable bundle | Same-user mutation or substitution of the live runtime root | Supervisor + runner + backend | Proposed three-part custody: finalized object with no writable aliases, retained genuine read-only descriptor, and governed raw-only FD-native attachment | Final descriptor `F_GETFL` access mode plus device/inode/type/permission/link/length/digest, every owned duplicate identity, guest-observed digest, and no pathname recovery | Create/open race, path/link/mapping/write/truncate, wrong/reused/writable FD, shared-offset/status behavior, caller-close/lifetime and path-fallback mutations, debugger/task-port attempt, runner/Supervisor crash | Runtime manifest digest, finalized custody record, transfer mechanism, patch/source identity, guest digest, recovery disposition | The FD-native patch candidate passed local custody, five deliberate mutations, and four owned unsandboxed guest digest runs with zero root-path opens. The public governed merge adds two no-guest raw-FD library-boundary tests and preserves the patch identity; it does not rerun or replace the owned-guest evidence. Exact signed installed App Sandbox/protected construction, task-port/grant, recovery, and final-byte corpus remain unproven | spike-observed |
| KERNEL-001 | Admitted guest-kernel bytes and hardening configuration equal the reviewed runtime profile and expose no undocumented kernel authority | Workload-to-kernel exploit, substituted kernel/config, unnecessary debug/module/device surface | Runtime maintainers + reviewers + Supervisor | Exact kernel image/config/boot/module/debug/launcher manifest; minimal required facilities; signed bundle and profile binding | Preflight digest/config identity and guest-observed inventory | Image/config/boot substitution, module loading, debug/inspection facilities, unnecessary drivers/protocols, launcher privilege/escape, deliberate restored facility | Kernel/source/config/patch digests, boot manifest, inventory and corpus result, limitations | Current pinned kernel is part of the runtime TCB but has no closed product hardening profile or retained minimization corpus; this defense-in-depth never replaces containment of a hostile guest kernel | proposed |
| RI-001 | Trusted peers match enrolled component identity | Same-user impersonation, partial update | macOS + Supervisor/Broker | XPC peer requirement, exact build/epoch checks | OS-enforced connection/message check and preflight | Unsigned, wrong ID/team, stale/debugged peer | RuntimeIntegrityAssessment | Apple Development/Developer ID and installed per-user crash/replacement matrices passed; distribution package, cross-session, logout/login, and product epoch store remain | spike-observed |
| KEY-001 | Daemon cannot use Approval or Supervisor evidence key | Authority collapse | Keychain + component identity | Component-specific security-epoch access group/access control | Failed key operation from daemon/stale component | Direct, injected, stale-release, and transition-crash attempts | Key authorization IDs/fingerprints; no secret material | Daemon/sibling and old/new group denial plus crash-safe transition observed; installed distribution/locked-Keychain/migration remain | spike-observed |
| DATA-001 | Daemon cannot retrieve user-only content | Agent content bypass | Content Broker | Separate store/API plus attempt-scoped handles | Credential/ACL/handle checks | MCP/daemon read, forged/stale/cross-job handle | Delivery audit record and content digest | Multi-process ledger denied daemon role and exposed only opaque references; signed product endpoint remains | spike-observed |
| DATA-002A | Broker snapshot bytes equal the selected/approved content identity before guest transport | Live original-file mutation, substitution, or retained mapping | Broker | Fresh Broker-owned snapshot/content object and manifest digest | Descriptor/snapshot identity and digest before grant/transfer | Original-file mutation, links/special files, retained writable descriptor/mapping, Broker crash | Source/input snapshot manifests and custody events | Broker descriptor/snapshot mechanics passed conditionally; product endpoint, installed store, and composition remain | spike-observed |
| DATA-002B | Guest source/input equals registered/approved snapshot bytes | Truncation, substitution, launcher/path staging race, or cross-attempt port confusion | Supervisor + trusted launcher + backend | Proposed separate fixed-cap attempt-bound directional envelopes and immutable launcher-to-child handoff | Per-role caps/binding/digest, exact byte accounting without EOF dependence, guest-observed digests, and child manifest | Partial/extra/wrong-order bytes, wrong/stale attempt, port swap, early child start/read, writer/reader/launcher crash, pathname substitution, child FD inheritance, cross-job reuse | Source/input manifests, port/child identities and bounds, transfer events, guest digests | Exact libkrun multiport transport, launcher binding/handoff, and installed-profile corpus are unproven; runtime-root custody is tracked separately by RUNTIME-002 | proposed |
| NET-001 | Guest has no unauthorized network/IPC | Exfiltration, metadata/host-service access | Supervisor + backend | Proven no-interface/network-none and no host socket mechanism | Exact config inspection plus guest probes | TCP/UDP/DNS/IPv4/6/loopback/Unix/vsock/metadata | Backend config/capability/validation digest | libkrun/HVF without NET and implicit vsock passed narrow probes. The later direct-block-root prototype also reported network unreachable and no usable vsock without a virtiofs device, but complete network/IPC, route-closure, installed-profile, and accepted-device evidence remains | spike-observed |
| VMM-001 | Exact guest-facing VMM device/parser surface is closed and accepted | Malformed guest device traffic reaches an unreviewed VMM parser, host route, or unbounded worker path | Supervisor + runner + backend | Closed typed runner/device list plus governed device removal or explicit acceptance of one exact residual surface | Build-feature and binary-route audit, guest inventory, exact device list, sanitizer/coverage record, attach denial | Restored host-directory APIs, malformed virtio queues/descriptors/FUSE opcodes and lengths, mapping requests, concurrency/reset/cancellation, resource exhaustion, VMM/reader failure | Backend/source/build/profile digests, device inventory, route-closure result, corpus/coverage/crash disposition, limitations | The [earlier P0-2 evidence](https://github.com/Shrimpworks/capsule-experiments/blob/0d8233b55f153b27a901a9ec45a3834208e3aa86/experiments/gate-c-libkrun-adversarial/NULLFS_P0_2.md) falsified minimal deletion. The [later disposition](https://github.com/Shrimpworks/capsule-experiments/blob/0d8233b55f153b27a901a9ec45a3834208e3aa86/experiments/gate-c-libkrun-adversarial/NULLFS_P0_2_DISPOSITION.md) selected `GOVERNED-PATCH` after a direct-block-root prototype exposed only balloon/RNG/console/block and reran 36 adversarial plus four identity cases without the original failure. The exact patch now appears in the unchanged public five-patch governed queue, but the follow-up merge ran no guest/VMM corpus. Removal is credible, not admitted: independent patch review, closed installed routes, mutation/sanitizer coverage, P0-1/P0-3 composition, and final signed bytes remain | unsupported |
| PLATFORM-001 | Isolation posture binds exact host platform and co-residency assumptions without claiming microarchitectural noninterference | Cross-domain speculative-execution, shared-cache, predictor, or other microarchitectural leakage | Release engineering + backend reviewers + Supervisor | Validation record binds hardware model, OS/hypervisor build, documented vendor mitigation state, and enforced concurrency/co-residency policy with explicit residual limitations | Preflight platform/configuration match and concurrency-policy enforcement | Mismatched host/OS, stale validation, forbidden concurrent guests, available cross-domain probe campaign, missing limitation in receipt/profile | Host/platform identity, validation record, concurrency policy/result, residual-risk statement | No exact mitigation mechanism or retained microarchitectural corpus is selected; validated-local currently trusts the hardware/virtualization stack and must not claim noninterference | proposed |
| RES-001 | Required resource controls are externally enforced exactly | Host availability loss | Supervisor + backend/host | Mechanism per supported limit dimension | Capability report and runtime observation | CPU, memory, PID, disk, output, cancellation abuse | Exact approved values, mechanism IDs, violation events | Fixed 4 KiB stream prefixes, wall/cancel scheduling, exact forced kill, fixed scratch image, integer vCPU, and closed RAM profiles were observed; CPU percentage/time, arbitrary RAM, total host memory, host pressure, and composed product enforcement remain unsupported/unvalidated | spike-observed |
| CLEAN-001 | Every created guest is destroyed or explicitly unresolved | Persistent hostile execution | Supervisor + backend | Durable handle/cleanup lease and reconcile lifecycle | Backend enumeration and teardown observation | Crash/cancel/timeout/orphan/partial destroy | Destroy/reconcile events and terminal class | Containerization failed; libkrun exact-process teardown and six installed reparent/reap cases passed with the enrolled launchd profile, but reboot/session/power-loss, failed-signal races, product store composition, and gVisor recovery remain. The `AttemptID`-keyed local fake fault/recovery corpus creates no guest and does not advance this guest-facing claim. | spike-observed |
| COMPLETE-001 | Ordinary success requires attempt-bound guest-reported completion and integrity evidence | False success after guest/bootstrap failure | Supervisor + distinct trusted launcher + backend | Dedicated fixed-cap typed completion/result port with commit trailer, closed launcher/child/runner FD manifests, and separate input/result/runtime/runner/teardown dispositions | Verify port/FD ownership, separate physical/JSON caps, cap-plus-one continuous drain without EOF dependence, binding, commit ordering after child-tree termination, bounded shutdown, hostile console-control/queue validation, and every required terminal element | Corrupt root, panic/OOM, missing executable, unchecked port ID/event, malformed queue/descriptor, wrong/duplicate/stale/forged/partial/oversized/trailing frame, early/missing commit, partial-then-error/zero host write, unexpected/swapped FD, reader stall/death, `SIGPIPE`, launcher crash before/after commit | Completion digest/type, port accounting, child/runner FD manifests, integrity/result checks, teardown result, guest-reported limitation | Multiple libkrun cases returned runner zero without workload completion. Independent Go/Node verification agrees on all [43 backend-independent vectors](https://github.com/Shrimpworks/capsule-experiments/blob/0d8233b55f153b27a901a9ec45a3834208e3aa86/experiments/gate-c-p0-3-protocol-conformance/RESULTS.md), six byte-exact encodings, exact caps, and local pipe fault dispositions. Public governed merge `cf0333cdba478cc34a8570a65b38412da7fd3ecc` fixes queued-backpressure shutdown and inactive-state defects; its no-guest corpus passed 51 default, 53 `blk`, four bounded property, and two raw-FD tests plus ASan, Clippy, repetition, and nine mutations. Four-file coverage is now 37/88 functions and 298/733 lines, with 2 functions/26 lines in `port.rs` and 14 lines in `process_tx.rs` still uncovered. It does not implement the typed port or distinct launcher or cover real guest/VMM/App Sandbox/teardown, independent review, caller flags, descriptor manifests, or guest-kernel attestation | proposed |
| EGR-002 | Only bounded safe artifacts leave hostile scratch storage | Symlink/special-file/parser exploit, sparse/size abuse, partial release | Broker + disposable parser | Fixed raw format/profile, bounded parser sandbox, fixed slots, limit-plus-one copy | Filesystem/profile checks and parser lifecycle reconciliation | Malformed ext4, links/devices/xattrs/ACLs, sparse/overlap, parser crash/timeout, partial copy | Parser identity/profile, artifact digest/length, rejection/cleanup class | Spike extractor rejected representative hostile files, but shell/`debugfs`/Docker tooling is non-product and malformed-filesystem fuzzing is open; this gate applies before file artifacts, not the first inline-JSON result | spike-observed |
| SUPPLY-001 | Admitted runtime bytes have complete attributable build and release identity | Dependency/source substitution, unreproducible or unreviewed bytes | Release engineering + trust verifier | Pinned materials, two-builder provenance, SBOM/source bundle, final signed-byte manifest | Admission checklist and exact installed readback | Moving inputs, path variance, missing materials, post-sign mutation, mixed package | Runtime manifest, provenance/SBOM/source digests, validation record | Path-remapped local rebuild feasibility passed. The governed `deno_core` follow-ups reproduced the exact binary/snapshot and then closed its standalone dynamic Bookworm root with a 22-entry snapshot-package-derived root and file-open/mutation evidence. The governed Deno and `rusty_v8` baseline branches are merged; the [fork-native check](https://github.com/Shrimpworks/capsule-experiments/blob/0d8233b55f153b27a901a9ec45a3834208e3aa86/experiments/gate-c-fork-native-deno-runtime-bundle/RESULTS.md) correctly blocked before building the intended Linux/arm64 target. Governed `rusty_v8` PR #4 later merged at exact head `80e863ddb942a4aa2b384e794fc23e35b9d2bb15`, and its [network-disabled ARM64 workflow](https://github.com/Shrimpworks/rusty_v8/actions/runs/30925045754) passed build/test/evidence collection and unsigned bundle upload. The fork construction blocker is closed, but current bytes still lack accepted fork-native Capsule reconstruction, independent builders, complete reviewed notices, a governed release, final signed manifest, clean-host floor, and exercised installed operations; runtime admission remains `BLOCKED`. A genuinely independent builder and paid clean-host matrix are viable but not currently planned; same-host/GitHub-CI evidence remains limited and those stronger evidence claims are deferred | spike-observed |
| PATCH-001 | Runtime patches are governed, reviewed, and removable | Patch drift or untracked security behavior | Runtime maintainers + reviewers | Governed fork or accepted upstream release with owner, digest, tests, rebase/removal record | Source/patch manifest and review state | Missing/reordered/modified patch, upstream rebase, partial build | Patch digests, upstream reference, review attestation | The public record now resides at `Shrimpworks/libkrun`, transferred from historical `dills122/libkrun` with PR history and exact identities intact. It preserves five exact patch files with aggregate identity `d19fd0ff159c699acccda2621519de45a09408bf3847b418ac34e02b79e805d5` from upstream `728df8125077d0db44265f6e997c72b81b65c015` to governed base `4ea8d1de861ed1c0636fc800b6da8fb71a086aa5`; follow-up head `8a2c91943793668f31a1cf7af431933be935bb58` merged as `cf0333cdba478cc34a8570a65b38412da7fd3ecc`. The transfer adds no evidence. Integration must pin exact commits/digests and verify ancestry, not trust movable `capsule/upstream-v1.19.4`. The source record establishes exact fork identity and pre-merge CI-tested library deltas, not reviewed release governance: the current baseline branch points to the follow-up merge while its verifier requires the earlier base, GitHub recorded no submitted independent/CODEOWNER review, and final ordering/source mapping, corresponding source, SBOM/provenance, advisory ownership, signing/distribution, and removal/rebase operations remain open | proposed |
| DIST-001 | Installed topology is signed, notarized, read back, and accepted on supported clean hosts | Substitution, quarantine/Gatekeeper failure, unsupported floor | Installer + Installation Trust Domain | Candidate complete notarized app with enrolled manifest and embedded per-user `SMAppService`; exact topology unproven | Staple/Gatekeeper/installation readback, service registration, inherited-descriptor behavior, and host compatibility | Missing ticket, translocation, partial install, old/new mix, service/recovery failure, unsupported OS | Package/runtime digests, ticket/assessment, installation epoch | P0-4A conditionally passed 18-role enumeration, 17 installed-entry readbacks, ad-hoc exact identity, per-user registration/explicit activation, refusal cases, and same-session recovery without a guest. Its App Sandbox runner failed before `main`. G3 exact discovery then proved that the certificate displayed with suffix `W4QUR9FUL4` actually carries subject OU and signed-byte TeamIdentifier `3DDR84M4JS`; all cached profiles are also 3DDR. No W4 app was built or installed and no ad-hoc/3DDR fallback was used. A matching W4 certificate/profile set, Team-enrolled reruns, complete-bundle signing/notarization/stapling/Gatekeeper, on-demand activation, clean hosts/sessions, final runtime bytes, and the effective macOS floor remain open | spike-observed |
| EGR-001 | Agent receives only fixed default summary | Content/metadata exfiltration | Broker + daemon | Fixed typed response with no guest strings | Response schema enforcement | Names/sizes/timing/errors/log/output injection | Summary schema/version and release decision | State/timing channel remains | proposed |
| TRUST-001 | Partial component update fails closed | Stale/mixed components | Installation Trust Domain | Signed manifest, common epoch binding, execution fence, component acceptance | Connection/preflight epoch comparison and durable-state/checkpoint reconciliation | Old/new combinations, process death, disk-full, WAL/corruption, partial/coherent restore | Epoch number/digest, transition/checkpoint, assessment result | Model/process evidence passed; product installer/store pending, coherent rollback needs stronger anchor | spike-observed |
| TUF-001 | External trust data is locally verified without live execution dependency | Malicious/stale service, rollback | Updater/trust verifier | Pinned TUF root plus signed local TrustSnapshot | Version/expiry/hash/checkpoint verification | Freeze, rollback, mix-and-match, delegated-scope abuse | Trust snapshot/checkpoint/freshness | Capsule defines revocation semantics | proposed |
| EVID-001 | User receipt contains attributable approval and enforcement claims | Daemon-forged success | Broker + Supervisor | Purpose-separated signatures over same registration/attempt | Independent signature and binding verification | Remove/swap/replay/cross-object evidence | Embedded grant, transcript, authorizations | Not platform attestation or proof of truth | proposed |

## Update rule

When a mechanism or spike changes, update the corresponding row with an evidence link, exact
version/configuration, and limitations. Never promote an entire backend because one row passes; all
claims required by the requested posture must be validated together.
