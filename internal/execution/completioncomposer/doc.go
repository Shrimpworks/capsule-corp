// Package completioncomposer implements an unwired, read-only terminal
// projection for the no-guest FakeBackend. It composes only Supervisor-retained
// attempt, completion-last, lifecycle, teardown, and authoritative-absence
// facts. It does not expose an endpoint, sign evidence, create a receipt, call
// a backend, or create a guest.
package completioncomposer
